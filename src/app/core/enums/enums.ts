export enum Difficulty {
    HARD = 1,
    MEDIUM,
    EASY
}

export enum FormType {
    ASSIGNMENT = 1,
    QUIZ
}

export enum QuestionType {
    MULTIPLE_CHOICE = 1,
    ESSAY,
    TRUE_OR_FALSE
}