export interface Student {
  id: string;
  accountId: string;
  email: string;
  phoneNumber: string;
  name: string;
  nationalId: string;
  address: string;
  gender: string;
  dateOfJoin: string; // ISO date string
}
